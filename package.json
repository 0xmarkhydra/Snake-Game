{
  "name": "snake-game",
  "version": "1.0.0",
  "description": "snake-game project with server and UI",
  "scripts": {
    "dev:backend": "cd backend && pnpm start:dev",
    "dev:ui": "cd ui && pnpm dev",
    "install:all": "pnpm install && cd backend && pnpm install && cd ../ui && pnpm install",
    "dev": "concurrently \"pnpm run dev:backend\" \"pnpm run dev:ui\""
  },
  "dependencies": {
    "canvas-confetti": "^1.9.4",
    "concurrently": "^9.1.2"
  }
}
